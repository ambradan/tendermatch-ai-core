# =============================================================================
# TenderMatch Backend - Variabili di Ambiente
# =============================================================================

# API Key Anthropic (OBBLIGATORIA)
# Ottieni la tua chiave da: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-YOUR_KEY_HERE

# Porta del server (default: 3000)
PORT=3000

# Origini CORS permesse (separare con virgola, senza spazi dopo la virgola)
# Esempio per produzione:
ALLOWED_ORIGINS=https://tendermatch.lovable.app,https://demo.tendermatch.it

# Per sviluppo locale:
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
